apiVersion: v1
kind: Service
metadata:
  name: opspilot
  labels:
    app: opspilot
spec:
  type: ClusterIP
  selector:
    app: opspilot
  ports:
    - name: http
      port: 8080
      targetPort: 8080
      protocol: TCP
